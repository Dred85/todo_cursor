global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: "tasker-backend"
    metrics_path: /metrics
    static_configs:
      - targets: ["tasker_backend:8000"]
        labels:
          service: "tasker"
